---
phase: 02-filter-pills
plan: 01
type: standard
---

<objective>
Add metal type filter pills (Gold/Silver/Platinum/All) to the collage page header that filter the radial gallery.
Purpose: Users can focus on specific metal types in their collection
Output: Working filter buttons that update gallery contents in real-time
</objective>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.0-ROADMAP.md
@bullion-tracker/src/app/collage/page.tsx
@bullion-tracker/src/components/ui/Badge.tsx
@bullion-tracker/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create FilterPills component</name>
  <action>Create src/components/gallery/FilterPills.tsx with Gold/Silver/Platinum/All toggle buttons using existing Badge styling patterns</action>
  <done>Component renders 4 buttons, accepts activeFilters and onFilterChange props</done>
  <verify>File exists, exports FilterPills component with correct TypeScript types</verify>
</task>

<task type="auto">
  <name>Integrate FilterPills into collage page</name>
  <action>Add FilterPills to collage page header, add useState for activeFilters, filter galleryItems based on selected metals</action>
  <done>Filter pills visible in header, clicking updates displayed items</done>
  <verify>npm run build succeeds, collage page renders with filter buttons</verify>
</task>

</tasks>

<verification>
- npm run build succeeds
- FilterPills component exported from gallery index
- Collage page shows filter buttons
- Filtering logic correctly shows/hides items by metal type
</verification>

<success_criteria>
- Gold/Silver/Platinum/All filter buttons visible in gallery header
- Clicking a metal filter shows only items of that metal type
- Clicking "All" shows all items
- Multiple filters can be selected (Gold + Silver shows both)
- Filter state persists during scroll
- Empty state shown if no items match filter
</success_criteria>

<output>
After completion, create 02-01-SUMMARY.md documenting:
- Filter component implementation details
- Integration approach
- Any design decisions made
</output>

<commit>
Per-task commits:
- feat(02-01): create FilterPills component
- feat(02-01): integrate filter pills into collage page

Metadata commit:
- docs(02-01): complete filter pills plan
</commit>

# Phase 25 Plan 02: Remove Web Dashboard Toggle

## Objective

Remove the Spot/Book toggle from the main web dashboard page.tsx, using unified portfolio value (totalBookValue).

## Current State

- `page.tsx` line 18: `const [valuationMode, setValuationMode] = useState<"spot" | "book">("spot");`
- `page.tsx` line 79: `const currentValue = valuationMode === "spot" ? totalMeltValue : totalBookValue;`
- `page.tsx` lines 341-350: Toggle UI with "Spot" and "Book" buttons
- AllocationPieChart and GainLossBarChart receive `valuationMode` prop (lines 922-923)

## Target State

- Remove valuationMode state
- Always use totalBookValue as the portfolio value
- Remove toggle UI
- Update chart components to not need valuationMode prop

## Tasks

### Task 1: Update page.tsx to Remove Toggle

**Files:**
- `bullion-tracker/src/app/page.tsx`

**Changes:**
1. Remove `valuationMode` state (line 18)
2. Change `currentValue` to always use `totalBookValue` (line 79)
3. Remove toggle UI section (lines 335-351)
4. Remove `valuationMode` prop from AllocationPieChart and GainLossBarChart
5. Remove unused ToggleButton component

### Task 2: Update AllocationPieChart to Remove valuationMode

**Files:**
- `bullion-tracker/src/components/charts/AllocationPieChart.tsx`

**Changes:**
1. Remove valuationMode prop
2. Always use book value calculation

### Task 3: Update GainLossBarChart to Remove valuationMode

**Files:**
- `bullion-tracker/src/components/charts/GainLossBarChart.tsx`

**Changes:**
1. Remove valuationMode prop
2. Always use book value calculation

## Verification

1. Web dashboard shows single "Portfolio Value" without toggle
2. Value calculation uses totalBookValue (spot + premium for bullion, guide price for numismatic)
3. Charts render without valuationMode prop
4. Build passes

## Time Estimate

~15 minutes
